import java.util.Collection;
import java.util.HashMap;

class Application{

    void getDuplicate(List<int> arr){

        

        Optional.ofNullable(arr)
        .stream()
        .flatMap(List::stream)
        .filter(Objects::nonNull);


        Comparable Comparator
        DEPT ID AND SALARY ARE SAME 


        SELECT EMP.NAME
        FROM EMPLOYEE EMP
        GROUP BY DEPT_ID, SALARY
        HAVING COUNT(*) > 1


        

    }

}